

            <div class="modal-items">
                <div class="flex-box">
                    <img width="50" src="{{ Voyager::image($medal->medal->image) }}">
                    <p>
                        {{ $medal->medal->title }}
                    </p>
                </div>
            </div>


